 class add {
     function __construct ($a,$b){
         return($a+$b);
     }
 }
 class base extend add {
     funciton __construct($ab,$b);
     {
         retrun($a+$b);
     }
 }
 $obj =  new base(20,30);
 cosnt a = 10;
 define("database","rajat")
trait a{
    fucntion add (){
        echo "hello";
    }
    class a {
        use add {
         $a =  int($a)
        }
    }
}

$a = 1;
$b = null;
if(isset($a)){
    
}
if(isset($b)){

}
fuction fac ($no,$f){
    $no = 5;
    $F= 1; 
    for($i = 0 $i <= $no; $i++){
        $f = $f*$i;
       
    }
    fac($no, $f);
}
$arr = array(1,2,6, 7, 4, 10)
<?php 

?>
We have a table employee with columns name, salary
Output - return employee name who has second highest salary
We have a table employee with columns name, salary
Output - return employee name who has second highest salary
'select top 1 sal from(select top 2 sal from emp order by desc)sub order by sal asc";
"select name,MAX(salary) from salary_tbl <("select MAX(salary) from salary")"